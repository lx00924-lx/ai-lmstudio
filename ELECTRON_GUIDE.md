# Electron 桌面端 (.exe) 打包指南

本项目已内置完整的 Electron 打包与运行配置，并支持 **自定义缓存与数据存储目录**。

---

## 1. 本地打包步骤（Windows 电脑）

### 第一步：安装 Electron 相关开发依赖
在本地项目根目录下打开终端（PowerShell 或 CMD），运行：
```bash
npm install -D electron electron-builder
```

### 第二步：执行一键打包构建
```bash
npm run electron:build
```

---

## 2. 打包产物说明

打包成功后，项目根目录下会自动生成 `release/` 文件夹：
* **`AI智能助手 Setup 0.0.10.exe`**：标准 Windows 安装向导程序（支持自定义安装路径、创建桌面快捷方式等）。
* **`AI智能助手 0.0.10.exe` (Portable)**：绿色免安装便携版，双击直接运行。

---

## 3. 桌面端自定义缓存与数据目录功能

在打包运行的桌面客户端中：
1. 点击左侧/顶部 **“应用设置”** 弹窗；
2. 找到 **【桌面端数据与缓存目录】** 区域：
   * **当前路径**：清晰显示当前应用数据与聊天记录存储的物理路径；
   * **更改目录**：唤起 Windows 原生文件夹选择器，可指定任意盘符（如 `D:\AI_Data`）；
   * **打开目录**：一键在 Windows 资源管理器中打开当前缓存文件夹；
   * **恢复默认**：一键还原至 Windows 默认 `%APPDATA%` 目录；
   * 选定新目录后点击 **“立即重启生效”**，系统会自动迁移生效。

---

## 4. 关键配置文件

* **主进程逻辑**：`electron/main.cjs`（负责启动路径重定向与 IPC 通信）
* **预加载脚本**：`electron/preload.cjs`
* **打包配置文件**：`electron-builder.json`
* **国内镜像加速**：`.npmrc`
